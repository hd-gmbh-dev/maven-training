# Exercise 5 — Building a Multi-Module Project

In this solution you can find the full multimodule application example.

Run the following commands to execute the application:

```shell
$ mvn install
$ mvn exec:java -pl app -Dexec.mainClass=com.example.app.Application
```
